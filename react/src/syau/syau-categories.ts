/** Integer index into `categoryColors` on the map and in parallel `memberCategory[]`. */
export type SyauCategory = number

/** Default SYAU game: category 0 = not yet named, 1 = named. */
export const syauCategoryUnnamed = 0
export const syauCategoryNamed = 1
