import createClient from 'openapi-fetch'

import type { paths } from './urbanstats-persistent-data' // generated by openapi-typescript

export const client = createClient<paths>({ baseUrl: 'https://persistent.urbanstats.org' })
